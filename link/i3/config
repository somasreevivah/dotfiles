#  General {{{1  #
##################


set $mod Mod1
set $windows Mod4
set $mod_r Mod5

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 11

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier Shift
focus_follows_mouse no

# start a terminal
bindsym $mod+Return exec ~/.dotfiles/bin/terminal.sh
bindsym $windows+Return exec "xterm"


# kill focused window
bindsym $mod+Shift+q kill


#scratchpad config 
bindsym $mod+Shift+minus move scratchpad
#show the first scratchpad window
bindsym $mod+minus scratchpad show

bindsym $mod+$windows+s exec "systemctl hybrid-sleep; ~/.dotfiles/bin/lock-screen.sh"
bindsym $mod+$windows+l exec "~/.dotfiles/bin/lock-screen.sh"
bindsym $mod+$windows+h exec "systemctl poweroff"

# Marks
bindsym $mod+m exec i3-input -l 1 -F 'mark %s' -P 'Mark name: '
bindsym $mod+g exec i3-input -l 1 -F '[con_mark=%s] focus' -P 'Goto mark: '

# program launcher
bindsym $mod+d exec ~/.dotfiles/bin/runner.sh
#bindsym $mod+d exec runner.sh
# program switcher
bindsym $mod+Shift+Tab exec ~/.dotfiles/bin/switcher.sh

# change focus
# It should be like in vim
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
# It should be like in vim
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+v split v

# split in vertical orientation
bindsym $mod+v split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

for_window [title="Papis document manager"] floating enable

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent
bindsym $mod+Shift+a focus child
bindsym $mod+b border toggle
#borders of windows will be default a pixel wide
new_window pixel
hide_edge_borders none

# Left hand mode
bindsym $windows+a focus left
bindsym $windows+s focus down
bindsym $windows+w focus up
bindsym $windows+d focus right
# Move containers
bindsym $windows+Shift+a move left
bindsym $windows+Shift+s move down
bindsym $windows+Shift+w move up
bindsym $windows+Shift+d move right
# Workspaces
bindsym $windows+e workspace next
bindsym $windows+q workspace prev

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r exec "~/.i3/i3status.sh" ; restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

bindsym $mod+Shift+o exec firefox --search "$(rofi -dmenu -p 'Search: ')"
#bindsym $mod+o exec "~/.dotfiles/bin/lib -d 'default_PICKER=dmenuSearcher'"
#bindsym $mod+Shift+o exec "~/.dotfiles/bin/lib -l | dmenu -fn '-bitstream-*-*-*-*-*-18-*-*-*-*-*-*-*' | xargs -n1 ~/.dotfiles/bin/lib -d 'default_PICKER=dmenuSearcher' "
#bindsym $mod+o exec "~/.dotfiles/bin/lib -d 'default_PICKER=dmenuSearcher' cmd "
bindsym $mod+o exec "python3 -m papis.main --picktool rofi --pick-lib gui --rofi"

bindsym $mod+Shift+p exec "~/.local/bin/rofi-pass"

# languages
#bindsym $windows+l exec "~/.dotfiles/bin/change_kb.sh $(~/.dotfiles/bin/change_kb.sh -l | dmenu -l 5 -p 'Select language')"
  # put default
bindsym $windows+Shift exec "~/.dotfiles/bin/change_kb.sh $(~/.dotfiles/bin/change_kb.sh -l | dmenu -l 5 -p 'Select language')"
bindsym $windows+Shift+space exec ~/.dotfiles/bin/change_kb.sh de-none


#wallpaper script
#exec_always ~/.dotfiles/bin/wallpaper.sh
bindsym $mod+Shift+w exec ~/.dotfiles/bin/wallpaper.sh

# composite manager
exec '~/.i3/scripts/composite.sh'

# dashboard
exec_once --no-startup-id "~/.dotfiles/bin/x11-dashboard.sh"
exec_always --no-startup-id "rofi -show window"


# focus the child container
#bindsym $mod+d focus child
#
bindsym $mod+i exec i3-input


#  X86 key bindings {{{1  #
###########################

 #{{{ All keys
#XF86ClearGrab
#XF86Switch_VT_1
#XF86Switch_VT_2
#XF86Switch_VT_3
#XF86Switch_VT_4
#XF86Switch_VT_5
#XF86Switch_VT_6
#XF86Switch_VT_7
#XF86Switch_VT_8
#XF86Switch_VT_9
#XF86Switch_VT_10
#XF86Prev_VMode
#XF86Next_VMode
#XF86Switch_VT_11
#XF86Switch_VT_12
#XF86Ungrab
#XF86AudioMute
#XF86PowerOff
#XF86LaunchA
#XF86Copy
#XF86Open
#XF86Paste
#XF86Cut
#XF86MenuKB
#XF86Calculator
#XF86Sleep
#XF86WakeUp
#XF86Explorer
#XF86Send
#XF86Xfer
#XF86Launch1
#XF86Launch2
#XF86WWW
#XF86DOS
#XF86ScreenSaver
#XF86RotateWindows
#XF86TaskPane
#XF86Mail
#XF86Favorites
#XF86MyComputer
#XF86Back
#XF86Forward
#XF86Eject
#XF86Eject
#XF86AudioNext
#XF86AudioPlay
#XF86AudioPrev
#XF86AudioStop
#XF86AudioRecord
#XF86AudioRewind
#XF86Phone
#XF86Tools
#XF86HomePage
#XF86Reload
#XF86Close
#XF86ScrollUp
#XF86ScrollDown
#XF86New
#XF86Launch5
#XF86Launch6
#XF86Launch7
#XF86Launch8
#XF86Launch9
#XF86AudioMicMute
#XF86TouchpadToggle
#XF86TouchpadOn
#XF86TouchpadOff
#XF86AudioPlay
#XF86AudioPause
#XF86Launch3
#XF86Launch4
#XF86LaunchB
#XF86Suspend
#XF86Close
#XF86AudioPlay
#XF86AudioForward
#XF86WebCam
#XF86Mail
#XF86Messenger
#XF86Search
#XF86Go
#XF86Finance
#XF86Game
#XF86Shop
#XF86AudioMedia
#XF86Display
#XF86KbdLightOnOff
#XF86KbdBrightnessDown
#XF86KbdBrightnessUp
#XF86Send
#XF86Reply
#XF86MailForward
#XF86Save
#XF86Documents
#XF86Battery
#XF86Bluetooth
#XF86WLAN }}}

bindsym XF86Tools exec espeak tools

#bindsym XF86TaskPane exec '~/.dotfiles/bin/rotate-screen.sh'
bindsym XF86TaskPane exec espeak 'hello world'
bindsym XF86PowerOff exec espeak 'brightness down'
bindsym XF86MonBrightnessDown exec xbacklight -dec 10
bindsym XF86MonBrightnessUp exec  xbacklight -inc 10
#bindsym XF86AudioLowerVolume exec ~/.dotfiles/bin/volume.sh -d
#bindsym XF86AudioRaiseVolume exec ~/.dotfiles/bin/volume.sh -u
bindsym XF86AudioRaiseVolume exec amixer -q sset Master 3%+ && pkill -RTMIN+10 i3blocks
bindsym XF86AudioLowerVolume exec amixer -q sset Master 3%- && pkill -RTMIN+10 i3blocks
bindsym XF86Search exec espeak search

#  Workspaces {{{1  #
#####################

# switch to workspace
workspace 1 output DP2
workspace 2 output DP2
workspace 3 output DP2
workspace 4 output DP2
workspace 5 output DP2
workspace 6 output DP1
workspace 7 output DP1
workspace 8 output DP1
workspace 9 output DP1
workspace 10 output DP1

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10
# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# More workspace related stuff
bindsym $mod+n workspace next
bindsym $mod+p workspace prev
bindsym $mod+Tab workspace back_and_forth
bindsym $mod+$windows+r exec i3-input -F 'rename workspace to "%s"' -P 'New name: '


#  Windows button key bindings {{{1  #
#####################################

bindsym $windows+t exec "date +'%H %M' | sed -e 's/^0\+//g' -e 's/\s0/ /g' | espeak"
bindsym $windows+p exec "import -window root $HOME/screenshot_$(date | tr ' ' '_').jpg "
#invert colors
bindsym $windows+i exec "xcalib -invert -alter"




#  disable mode {{{1  #
#######################


mode "disable" {


    # back to normal: Escape
    bindsym Escape mode "default"
}

bindsym $mod+Ctrl+d mode "disable"

#  move mode {{{1  #
####################

mode "move" {

    bindsym c move position center

    bindsym Shift+h move left 10px
    bindsym Shift+j move down 10px
    bindsym Shift+k move up 10px
    bindsym Shift+l move right 10px

    bindsym h move left 10px
    bindsym j move down 10px
    bindsym k move up 10px
    bindsym l move right 10px

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $windows+m mode "move"

#  Resize {{{1  #
#################

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym l resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym j resize shrink height 10 px or 10 ppt
        bindsym h resize grow width 10 px or 10 ppt
        # Resize more
        bindsym Shift+l resize shrink width 30 px or 30 ppt
        bindsym Shift+k resize grow height 30 px or 30 ppt
        bindsym Shift+j resize shrink height 30 px or 30 ppt
        bindsym Shift+h resize grow width 30 px or 30 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"



#  Status bar {{{1  #
#####################

bindsym $mod+Shift+b bar mode toggle
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        #mode invisible
        position top
        status_command ~/.dotfiles/bin/i3status.sh
        colors {
                background #000000
                #statusline #ffffff

                separator #555555
                #active_workspace   #2c3e50 #2c3e50 #1abc9c
                #focused_workspace  #2c3e50 #2c3e50 #1abc9c
                #inactive_workspace #2c3e50 #2c3e50 #ecf0f1
                #urgent_workspace   #e74c3c #e74c3c #ecf0f1
        }
}


#  Theme {{{1  #
################

# Colors
# # class          border  backgr. text    indicator
#client.focused   #2c3e50 #2c3e50 #1abc9c #2e9ef4
#client.focused    #2c3e50 #ffffff #000000 #2e9ef4
client.focused    #2c3e50 #ff0000 #000000 #2e9ef4
#client.unfocused #2c3e50 #2c3e50 #1abc9c #2e9ef4
#client.urgent    #e74c3c #e74c3c #ffffff #900000

# vim:foldmethod=marker
